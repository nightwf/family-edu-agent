server {
    listen 80;
    server_name heyaagent.top;

    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
        default_type text/plain;
    }

    include /etc/nginx/family-edu-location.conf;
}
